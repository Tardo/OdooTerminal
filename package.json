{
  "scripts": {
    "devel": "export NODE_ENV=development && rollup -c -w",
    "tests": "export NODE_ENV=development && rollup -c && poetry run ./tools/release.py && poetry run pytest",
    "build": "export NODE_ENV=production && rollup -c && poetry run ./tools/release.py",
    "clean": "rm -r ./src/dist"
  },
  "dependencies": {
    "@rollup/plugin-alias": "^3.1.9",
    "@rollup/plugin-node-resolve": "^15.1.0",
    "autoprefixer": "^10.4.0",
    "cssnano": "^5.0.12",
    "rollup-plugin-analyzer": "^4.0.0",
    "rollup-plugin-postcss": "^4.0.2",
    "rollup-plugin-terser": "^7.0.2"
  }
}
